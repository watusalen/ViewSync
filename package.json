{
  "name": "view-sync-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:desktop": "npm run dev --workspace=view-sync-desktop",
    "build:all": "npm run build --workspaces"
  },
  "devDependencies": {
    "puppeteer": "^24.42.0"
  }
}
